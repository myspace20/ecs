version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "java-web-service"  # just the family name
        LoadBalancerInfo:
          ContainerName: "java-web-app"
          ContainerPort: 8080
